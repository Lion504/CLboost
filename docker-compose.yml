version: '3.8'

services:
  app:
    build: .
    ports:
      - '8080:8080'
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=8080
      # Database configuration (uncomment when ready to use MariaDB)
      # - DB_HOST=db
      # - DB_PORT=3306
      # - DB_NAME=clbooster
      # - DB_USERNAME=root
      # - DB_PASSWORD=password
    # depends_on:
    #   - db

  # MariaDB database (uncomment when ready to use database)
  # db:
  #   image: mariadb:10.11
  #   restart: unless-stopped
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=password
  #     - MYSQL_DATABASE=clbooster
  #     - MYSQL_USER=clbooster
  #     - MYSQL_PASSWORD=password
  #   ports:
  #     - '3306:3306'
  #   volumes:
  #     - mariadb_data:/var/lib/mysql

# volumes:
#   mariadb_data:
